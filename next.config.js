/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['dummyimage.com', 'djmag.com', 'images2.imgbox.com']
  }
}

module.exports = nextConfig
